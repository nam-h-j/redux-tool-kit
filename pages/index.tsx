import type { NextPage } from "next";
import Button from "../component/atoms/Button";

const Home: NextPage = () => {
  return (
    <div>
      <Button>
        <div>button here</div>
      </Button>
    </div>
  );
};

export default Home;
